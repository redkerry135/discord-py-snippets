# color.py

A snippet to create or change color roles, with the role name being the user's username.

Note:
- Only hex colors are supported, with the hash
- Roles do not update their names when users change their usernames - this must be changed manually
